doctype html
html
    head
        block head
        title TSS Help Center
        != css('bootstrap.min')
        != css('app')
        link(href='http://fonts.googleapis.com/css?family=Pontano+Sans' rel='stylesheet' type='text/css')

      
    body(ng-app="tssHelp")
        block content

        != js('vendor/jquery.min')
        != js('vendor/bootstrap.min')     
        != js('vendor/angular.min')
        != js('vendor/angular-ui-router.min')
        != js('vendor/angular-sanitize.min')
        != js('app')

  
        script(src='partials/ad-types/ad-types.js')

